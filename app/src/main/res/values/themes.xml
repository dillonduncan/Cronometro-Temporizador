<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Cronometro" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Cronometro" parent="Base.Theme.Cronometro" />

    <style name="btn_style_rojo" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/btn_style_rojo</item>
        <item name="backgroundTint">@color/rojo</item>
    </style>

    <style name="btn_style_dorado" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/btn_dorado_style</item>
        <item name="backgroundTint">@color/dorado</item>
    </style>
</resources>